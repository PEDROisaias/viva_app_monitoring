name: viva_app_monitoring
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  crypto: ^3.0.7
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_local_notifications: ^20.1.0
  mqtt_client: ^10.11.11
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

flutter_launcher_icons: 
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/viva_logo.png"

  assets: 
    - assets/images/viva_logo.png
